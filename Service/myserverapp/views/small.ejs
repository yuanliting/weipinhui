<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title %></title>
    <script src="/javascripts/jquery.js"></script>
</head>
<body>
<h1>这是小分类页面</h1>
<form action="">
    小分类名字<input type="text" name="ClassifyName" required><br>
    小分类的描述: <input type="text" name="describe" required><br>
    它属于 : <input type="text" name="belongTo" required>大分类的类型<br>
    它的超链接: <input type="text" name="superLink" required><br>
    小分类的图片: <input type="file" name="photo" /><br /><br>
    <button type="submit">提交</button>
</form>
</body>
<script>
    $('form').submit(function (ev) {
        ev.preventDefault();
        var data = new FormData(this);
        $.post({
            url:'/small',
            data:data,
            contentType:false,
            processData:false,
            success:function (resData) {
                if(resData.error == 0){
//                    console.log(resData);
                    alert(resData.message);
                   location.href = '/';
                }else{
                    alert(resData.message);
                    location.href = '/small';
                }
            }
        })
    })
</script>
</html>